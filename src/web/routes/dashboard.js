import express from 'express'; import { renderDashboard } from '../views/dashboard.js';
/** @param {{config:any,auth:any,activityStore:any,athleteService:any,sessions:any,logger:any}} deps */
export function dashboardRouter({ config, auth, activityStore, athleteService, sessions, logger }) { const router = express.Router(); const view = (/** @type {any} */ req, /** @type {any} */ overrides={}) => renderDashboard({ config, athlete:req.session.athlete, recent:activityStore.recentFor(req.session.athlete.athlete_id,5), csrfToken:sessions.csrfToken(req.session.cookieValue), ...overrides }); router.get('/dashboard',auth.requireAthlete,(/** @type {any} */ req,res)=>res.type('html').send(view(req))); router.post('/dashboard/message',auth.requireAthlete,auth.requireCsrf,(/** @type {any} */ req,res)=>{ const result=athleteService.updateMessage(req.session.athlete.athlete_id,req.body?.message); return result.ok?res.redirect(302,'/dashboard'):res.status(400).type('html').send(view(req,{notice:result.error,messageValue:req.body?.message})); }); router.post('/disconnect',auth.requireAthlete,auth.requireCsrf,async(/** @type {any} */ req,res)=>{ await athleteService.disconnect(req.session.athlete.athlete_id); res.clearCookie(sessions.COOKIE_NAME,{path:'/'}); return res.redirect(302,'/login'); }); return router; }

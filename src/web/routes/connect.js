import crypto from 'node:crypto'; import express from 'express'; import { validateMessage } from '../../domain/message.js'; import { renderConnectPage, renderInvalidInvite } from '../views/connect.js';
export const STATE_TTL_SECONDS = 600; export const INVITE_TTL_SECONDS = 604800;
/** @param {any} config @param {string} state */ const authorizeUrl = (config, state) => `https://www.strava.com/oauth/authorize?${new URLSearchParams({ client_id: config.clientId, redirect_uri: `${config.baseUrl}/oauth/callback`, response_type: 'code', approval_prompt: 'auto', scope: 'activity:read,activity:write', state })}`;
/** @param {{config:any,inviteStore:any,authStateStore:any,clock:any,logger:any}} deps */
export function connectRouter({ config, inviteStore, authStateStore, clock, logger }) { const router = express.Router(); const begin = (/** @type {any} */ res, /** @type {any} */ state) => { const value = crypto.randomBytes(32).toString('hex'); authStateStore.create({ state: value, ...state, now: clock.now(), expiresAt: clock.now() + STATE_TTL_SECONDS }); return res.redirect(302, authorizeUrl(config, value)); }; router.get('/connect', (req,res) => { const invite = inviteStore.getUsable(String(req.query.invite ?? ''), clock.now()); return invite ? res.type('html').send(renderConnectPage({ config, inviteToken: invite.token })) : res.status(403).type('html').send(renderInvalidInvite()); }); router.post('/connect', (req,res) => { const token = String(req.body?.invite ?? ''); if (!inviteStore.getUsable(token, clock.now())) return res.status(403).type('html').send(renderInvalidInvite()); const valid = validateMessage(req.body?.message); return valid.ok ? begin(res, { inviteToken: token, pendingMessage: valid.value }) : res.status(400).type('html').send(renderConnectPage({ config, inviteToken: token, message: req.body?.message, error: valid.error })); }); router.get('/login', (_req,res) => begin(res, { inviteToken: null, pendingMessage: null })); return router; }
